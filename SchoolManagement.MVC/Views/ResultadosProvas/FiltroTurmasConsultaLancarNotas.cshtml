@model SchoolManagement.MVC.ViewModels.TurmaViewModel

@{
    ViewBag.Title = "Filtro Turmas Professor Leciona";
}

<style>
    .example-modal .modal {
        position: relative;
        top: auto;
        bottom: auto;
        right: auto;
        left: auto;
        display: block;
        z-index: 1;
    }

    .example-modal .modal {
        background: transparent !important;
    }
</style>

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            Notas
            <small>Turmas que Leciono</small>
        </h1>
        <ol class="breadcrumb">
            <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
            <li><a href="#">Notas</a></li>
            <li class="active">Turmas que Leciono</li>
        </ol>
    </section>

    <!-- Main content -->
    <section class="content">
        <div class="example-modal">
            <div class="modal">
                <div class="modal-dialog">
                    @using (Html.BeginForm("FiltroProvasConsultaLancarNotas", "ResultadosProvas", FormMethod.Get))
                    {
                        <div class="modal-content">
                            <div class="modal-header">
                                <h4 class="modal-title">Filtro Turmas que Leciono</h4>
                            </div>
                            <div class="modal-body">

                                <div class="form-group col-md-12">
                                    @Html.LabelFor(m => m.ListaTurmas)

                                    @{
                        List<SelectListItem> listaTurmasConsulta = new List<SelectListItem>();
                        foreach (var item in @ViewBag.ListaTurmasConsulta)
                        {
                            SelectListItem selec = new SelectListItem()
                            {
                                Value = item.Value,
                                Text = item.Text
                            };
                            listaTurmasConsulta.Add(selec);
                        }
                        @Html.DropDownListFor(m => m.TurmaId, listaTurmasConsulta, new { @class = "form-control", placeholder = "Selecione uma Turma", style = "width: 100%;", id = "turmas" })
                                    }

                                    @Html.ValidationMessageFor(model => model.ListaTurmas)
                                </div>

                            </div>
                            <div class="modal-footer">
                                <button type="submit" class="btn btn-primary">Avançar</button>
                            </div>
                        </div><!-- /.modal-content -->
                    }
                </div><!-- /.modal-dialog -->
            </div><!-- /.modal -->
        </div><!-- /.example-modal -->
    </section><!-- /.content -->
</div><!-- /.content-wrapper -->
