@model IEnumerable<SchoolManagement.MVC.ViewModels.AnoLetivoViewModel>

@{
    ViewBag.Title = "VisualizarAnoLetivo";
}

<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            Ano Letivo
            <small>Consulta de Ano Letivo</small>
        </h1>
        <ol class="breadcrumb">
            <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
            <li class="active">Ano Letivo</li>
            <li class="active">Consulta de Ano Letivo</li>
        </ol>
    </section>

    <!-- Main content -->
    <section class="content">
        <div class="row">
            <!-- left column -->
            <div class="col-md-12">
                <div class="box box-default">
                    <div class="box-header with-border">
                        <h3 class="box-title">Resultado de Consulta Alunos</h3>
                    </div><!-- /.box-header -->
                    <div class="box-body">

                        <table class="table">
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.QntUnidades)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Ano)
                                </th>
                                <th></th>
                            </tr>

                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.QntUnidades)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Ano)
                                    </td>
                                    <td>
                                        @Html.ActionLink("Editar", "Edit", new { id = item.AnoLetivoId }) |
                                        @Html.ActionLink("Detalhes", "Details", new { id = item.AnoLetivoId }) |
                                        @Html.ActionLink("Delete", "Delete", new { id = item.AnoLetivoId })
                                    </td>
                                </tr>
                            }

                        </table>
                    </div><!-- /.box-body -->
                </div>
            </div><!--/.col (left) -->
            <!-- right column -->
        </div>   <!-- /.row -->
    </section><!-- /.content -->
</div><!-- /.content-wrapper -->